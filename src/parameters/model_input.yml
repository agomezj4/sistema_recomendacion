## Model Input parameters

# Parámetros para train_test_split_pl
train_test_split: {
    'test_size': 0.1,
    'validation_size': 0.1,
    'random_state': 42,
    'shuffle': True
}

numeric_cols:
 - R_calificacion
 - F_calificacion
 - M_calificacion